{
  "name": "hp-remote-server",
  "version": "1.0.0",
  "description": "Relay server for hp_remote: pairs an Android device with a web controller and forwards screen frames / input events between them.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ws": "^8.18.0"
  }
}
